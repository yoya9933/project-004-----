# 2026-07-07 建置日誌

## 工作摘要

- 讀取使用者附件需求，確認本次要在 `project-004-工程分析` 內實作。
- 補建項目總覽與標準子目錄。
- 保存使用者需求附件到 `02_輸入資料/`。
- 以既有 `05_測試與驗證/test_issue_features.csv` 生成 26 筆 `06_交付物/penalty_cases.csv`。
- 建立可交報告版腳本：
  - `04_執行稿/analyze_penalty_cases_report.py`
- 建立進階模型版腳本：
  - `04_執行稿/run_penalty_linear_model.py`
- 建立主表生成腳本：
  - `04_執行稿/build_penalty_cases_starter.py`
- 建立交付說明與驗證紀錄。

## 重要判斷

- 目前不把既有金額候選清單自動轉為 `contract_price`、`claimed_penalty` 或 `allowed_penalty`。
- 原因是同一判決會出現多個工程款、擔保金、鑑定金額、修復費用與違約金，未人工判讀前無法確定法律意義。
- 因此本次交付以「標註主表骨架＋可重跑分析流程＋安全停止模型」為主。

## 驗證結果

- `build_penalty_cases_starter.py`：成功輸出 26 筆主表與欄位字典。
- `analyze_penalty_cases_report.py`：成功輸出缺值檢查、清理表、空分組統計、空爭點比較與 Markdown 報告。
- `run_penalty_linear_model.py`：成功偵測沒有完整模型列並安全跳過，未產生誤導性係數。

## 待補事項

- 人工補齊 `contract_price`、`delay_days`、`claimed_penalty`、`allowed_penalty`。
- 人工補齊建議爭點欄位：
  - `issue_actual_damage_unclear`
  - `issue_partial_completion`
  - `issue_owner_fault`
  - `issue_contractor_fault`
  - `issue_extension_request`
  - `issue_used_by_owner`
- 補標後重跑可交報告版與進階模型版。

## 可提升至全域日誌的工具經驗

- 事件：在沙盒中執行 `uv run python` 時，`uv` 預設嘗試存取使用者層級 `AppData` 快取與 Python 安裝鎖，造成權限錯誤。
- 修正：同時設定 `$env:UV_CACHE_DIR=(Resolve-Path '.uv-cache').Path` 與 `$env:UV_PYTHON_INSTALL_DIR=(Resolve-Path '.uv-python').Path`，讓 cache 與 Python 安裝目錄都落在項目內。
- 補充：本次曾嘗試依治理規則寫入全域複利與踩坑日誌，但寫入 project004 外部檔案的升權請求被系統用量/審核限制擋下；因此先保留於本地日誌，待有權限時再提升。
