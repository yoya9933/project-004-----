# 2026-07-07 資料整理紀錄


## 範圍

- 整理根目錄散落的資料、分析結果、執行腳本、測試資料與暫存快取。
- 保留 `00_項目總覽.md` 在根目錄作為索引。
- 保留 `.agents`、`.git`、`.uv-cache`、`.uv-python`、`.vscode` 等工具或環境資料夾原位不動。
- 未找到適用的 `AGENTS.md`；本次依既有編號資料夾結構整理。

## 搬移對應

| 目標資料夾 | 搬入內容 |
|---|---|
| `02_輸入資料` | `2021-2026判決總表.csv`、`法律課程資料庫-20260707T080337Z-3-001.zip`、`法律課程資料庫-20260707T080337Z-3-001/`、`json_output/`、`pdf_output/` |
| `03_研究與分析` | `money_feature_amounts_long.csv`、`money_feature_amounts_summary.csv`、`money_feature_amounts_wide.csv`、`tpot_issue_model_details.json`、`tpot_issue_model_summary.csv`、`tpot_issue_strict_linear_details.json`、`tpot_issue_strict_linear_summary.csv` |
| `04_執行稿` | `download_judgment_json.py`、`download_judgment_pdf.py`、`extract_money_feature_amounts.ps1`、`run_tpot_issue_model_search.py`、`run_tpot_issue_strict_linear_search.py` |
| `05_測試與驗證` | `test_issue_features.csv`、`json_output_test/`、`test/` |
| `99_封存` | `__pycache__/` |

## 整理後概況

- 根目錄只保留總覽檔與主要分類資料夾。
- `02_輸入資料` 存放原始判決資料、下載輸出與 PDF/JSON 輸入資料。
- `03_研究與分析` 存放特徵萃取與 TPOT 模型搜尋結果。
- `04_執行稿` 存放可重跑的 Python/PowerShell 腳本。
- `05_測試與驗證` 存放測試輸出與驗證素材。
- `99_封存` 存放可再生或暫存類內容。

## 腳本路徑同步

- `04_執行稿/download_judgment_json.py` 預設輸出到 `02_輸入資料/json_output`。
- `04_執行稿/download_judgment_pdf.py` 預設讀取 `02_輸入資料/2021-2026判決總表.csv`，並輸出到 `02_輸入資料/pdf_output`。
- `04_執行稿/extract_money_feature_amounts.ps1` 預設讀取 `05_測試與驗證/test`，並輸出到 `03_研究與分析`。
- `04_執行稿/run_tpot_issue_model_search.py` 與 `04_執行稿/run_tpot_issue_strict_linear_search.py` 預設讀取 `05_測試與驗證/test_issue_features.csv`，並輸出到 `03_研究與分析`。
