# 2026-07-07 實作驗證紀錄

## 驗證環境

- 使用 `uv run --python 3.12` 建立本項目分析環境。
- 套件：
  - `pandas`
  - `matplotlib`
  - `scikit-learn`
  - `tabulate`

## 已執行指令

```powershell
$env:UV_CACHE_DIR=(Resolve-Path '.uv-cache').Path
$env:UV_PYTHON_INSTALL_DIR=(Resolve-Path '.uv-python').Path
uv run --with pandas --python 3.12 python "04_執行稿\build_penalty_cases_starter.py"
```

結果：

- 成功輸出 `06_交付物/penalty_cases.csv`
- 成功輸出 `06_交付物/penalty_cases_data_dictionary.csv`
- 列數：26

```powershell
$env:UV_CACHE_DIR=(Resolve-Path '.uv-cache').Path
$env:UV_PYTHON_INSTALL_DIR=(Resolve-Path '.uv-python').Path
uv run --with pandas --with matplotlib --with tabulate --python 3.12 python "04_執行稿\analyze_penalty_cases_report.py"
```

結果：

- 成功輸出 `06_交付物/analysis_report_version/penalty_cases_cleaned.csv`
- 成功輸出 `06_交付物/analysis_report_version/penalty_cases_missingness.csv`
- 成功輸出 `06_交付物/analysis_report_version/penalty_ratio_group_summary.csv`
- 成功輸出 `06_交付物/analysis_report_version/issue_reduction_comparison.csv`
- 成功輸出 `06_交付物/analysis_report_version/工程逾期違約金酌減因素探索分析.md`

```powershell
$env:UV_CACHE_DIR=(Resolve-Path '.uv-cache').Path
$env:UV_PYTHON_INSTALL_DIR=(Resolve-Path '.uv-python').Path
uv run --with pandas --with scikit-learn --with tabulate --python 3.12 python "04_執行稿\run_penalty_linear_model.py"
```

結果：

- 成功輸出 `06_交付物/advanced_linear_model/model_status.json`
- 成功輸出 `06_交付物/advanced_linear_model/線性模型輔助分析狀態.md`
- 模型狀態：`skipped`
- 原因：目前沒有完整模型列，`delay_days`、`penalty_to_contract` 與建議人工爭點欄位尚未補齊。

## 驗證結論

- 三支腳本均可執行並產出預期檔案。
- 目前可交付的是「標註主表骨架、欄位字典、分析流程與限制報告」。
- 待人工補齊數值與爭點後，報告版會自動產生分組統計與散佈圖；進階模型版會在資料條件足夠時產生 Logistic Regression 係數表。
